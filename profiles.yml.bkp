# Snowflake connection — same settings as dbt_fusion_tuto (profile: projet8_ocr)
# For local dbt: copy to ~/.dbt/profiles.yml OR set DBT_PROFILES_DIR to this project folder

projet8_ocr:
  target: dev
  outputs:
    dev:
      type: snowflake
      threads: 16
      account: ARLHWOC-BM40687
      user: YUKEL
      password: pucNivruvhi9nadxeg
      role: accountadmin
      database: analytics
      warehouse: compute_wh
      schema: analytics_prod

default:
  target: dev
  outputs:
    dev:
      type: snowflake
      threads: 16
      account: ARLHWOC-BM40687
      user: YUKEL
      password: pucNivruvhi9nadxeg
      role: accountadmin
      database: analytics
      warehouse: compute_wh
      schema: analytics_prod
